# The ABL rewrites console= to console=null and appends quiet, so kernel
# messages never reach the display or a serial console. earlycon at least gets
# the early boot out over the framebuffer the bootloader leaves enabled.
earlycon
