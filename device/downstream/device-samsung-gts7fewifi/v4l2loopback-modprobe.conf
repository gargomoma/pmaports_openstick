# Two loopback nodes, one per sensor (video10 front, video11 rear), driven by one bridge
# process. exclusive_caps hides an idle node's capture side. The label list is quoted once.
options v4l2loopback video_nr=10,11 card_label="Tablet Front Camera,Tablet Rear Camera" exclusive_caps=1,1
