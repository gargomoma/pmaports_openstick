# polkitd drops privileges and then cannot read its own /proc/self/fdinfo for the pidfd check
# (dumpable 0), so every unix-process subject fails. 1 keeps it dumpable; 2 does not help.
fs.suid_dumpable=1
