# /usr/share/battery-protect/battery-protect.conf
# Configuration for Samsung tablet Protect Battery service

# Stop charging when capacity reaches MAX_CHARGE (percentage)
MAX_CHARGE=90

# Resume charging when capacity drops to RESUME_CHARGE (percentage)
RESUME_CHARGE=85

# Polling interval in seconds
POLL_INTERVAL=30
